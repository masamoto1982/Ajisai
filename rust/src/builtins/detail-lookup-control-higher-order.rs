// rust/src/builtins/detail-lookup-control-higher-order.rs
//
// Detailed documentation for control structure and higher-order function words

/// Returns detailed documentation for control structure and higher-order function words.
pub(crate) fn lookup_detail_control_higher_order(name: &str) -> Option<String> {
    let result = match name {
        // ============================================================================
        // 制御構造
        // ============================================================================

        ":" => r#"# - コードブロック開始

## 機能
コードブロック（遅延評価されるコード）の開始を示します。
{ } または ( ) で囲んで使用し、DEFでのワード定義に使用します。

## 使用法
{ コード } 'NAME' DEF

## 使用例
{ [ 2 ] * } 'DOUBLE' DEF
[ 5 ] DOUBLE    # → [ 10 ]

{ [ 800 1200 ] MUSIC@CHORD } 'VOWEL_A' DEF

## 注意
- 必ず } または ) で閉じる必要があります
- ネスト可能です"#,

        ";" => r#"# | - コードブロック終了

## 機能
コードブロックの終了を示します。
と対で使用します。

## 使用法
コード |

## 注意
- 対応する がない場合はエラーになります"#,

        "ROUTE" => r#"# ROUTE - フロー経路制御

## 機能
条件-アクションのコードブロックペアに基づき、フローを条件に合致する経路へ振り分けます。
分岐（if-else）と反復（while）を修飾子で切り替えられます。

## 使用法
条件| :アクション| :デフォルト| ROUTE     # 分岐（. ROUTE）
条件| :アクション| .. ROUTE               # 反復（.. ROUTE）
条件| :アクション| :デフォルト| ,, ROUTE  # 分流（,, ROUTE）

## 引数
- コードブロックのペア（条件＋アクション）を上から順に評価
- コードブロックの総数が奇数 → 最後の1つはデフォルトアクション
- コードブロックの総数が偶数 → デフォルトなし（全条件不成立ならフロー素通り）

## 修飾子
- `.` ROUTE（デフォルト）: 1回の経路選択（if-else / case）
- `..` ROUTE: 循環。条件成立中アクションを繰り返す（while / loop）
- `,,` ROUTE: 分流。元のフローも残す
- `.. ,,` ROUTE: 循環＋分流

## 使用例

### 分岐（. ROUTE = デフォルト）
# 符号判定
[ -5 ]
  ,, [ 0 ] < |   [ -1 ] |
  ,, [ 0 ] = |   [ 0 ] |
                    [ 1 ] |
  ROUTE
# → [ -1 ]

### 反復（.. ROUTE）
# 2倍を繰り返して1000を超えたら停止
[ 1 ]
  ,, [ 1000 ] < |  [ 2 ] * |
.. ROUTE
# → [ 1024 ]

### 分流（,, ROUTE）
[ 5 ]
  ,, [ 0 ] < |  [ -1 ] |
                   [ 1 ] |
,, ROUTE
# → [ 5 ] [ 1 ]

## 注意
- 条件ブロックは ,, を使い元フローを保持しつつ判定するのが一般的
- `..` ROUTE の反復上限は10,000回
- TIMES と >> / >>> の代替です"#,

        "==" => r#"# == - パイプライン演算子

## 機能
データフローを視覚的に明示するためのマーカーです。
実行時には何も行いません（no-op）。

## 使用法
value == operation

## 使用例
# 単一行
[ 1 2 3 ] == [ 2 ] * | MAP

# 複数行でデータフローを明示
[ 1 2 3 4 5 ]
  == [ 2 ] * | MAP           # 各要素を2倍
  == [ 5 ] < NOT | FILTER    # 5より大きいものを抽出
  == [ 0 ] + | FOLD          # 合計
# → [ 24 ]  (6+8+10)

## 注意
- 純粋に視覚的なマーカーであり、動作には影響しません
- 省略しても結果は同じです"#,

        "=>" => r#"# => - Nil Coalescing演算子

## 機能
値がNILの場合に代替値を返します。
他言語の ?? (C#, JavaScript) や ?: (Kotlin) に相当します。

## 使用法
value => default
→ valueがNILならdefault、そうでなければvalue

## 使用例
NIL => [ 0 ]                    # → [ 0 ]
[ 42 ] => [ 0 ]                 # → [ 42 ]

# 範囲外アクセスのフォールバック
[ 1 2 3 ] [ 10 ] GET => [ 0 ]   # → [ 0 ]（GETがNILを返す）
[ 1 2 3 ] [ 0 ] GET => [ 0 ]    # → [ 1 ]（GETが値を返す）

# 連鎖
value => fallback1 => fallback2

# FILTERの結果がNILの場合
[ 1 2 3 ] [ 10 ] < NOT | FILTER => [ 0 ]
# → [ 0 ]（該当なしでNIL）

## 注意
- NILのみを置換します（FALSEは置換しません）
- 左結合です（左から順に評価）"#,

        // ============================================================================
        // 高階関数
        // ============================================================================

        "MAP" => r#"# MAP - 各要素に関数を適用

## 機能
ベクタまたはスタックの各要素に対して、指定したコードを適用します。

## 使用法（StackTopモード）
[ 要素... ] code | MAP              # コードブロックを直接指定
[ 要素... ] 'WORD' MAP                # ワード名で指定
→ 各要素にコードを適用した結果のベクタ

## 使用法（Stackモード）
要素... [ 個数 ] code | .. MAP
→ 指定個数の要素にコードを適用

## 使用例
[ 1 2 3 4 ] [ 2 ] * | MAP
# → [ 2 4 6 8 ]  (各要素を2倍)

[ 1 2 3 ] [ 1 ] + | MAP
# → [ 2 3 4 ]  (各要素に1を加算)

# カスタムワードを使用
[ 2 ] * [ 1 ] + | 'DOUBLE_PLUS_ONE' DEF
[ 1 2 3 ] 'DOUBLE_PLUS_ONE' MAP
# → [ 3 5 7 ]  (各要素を2倍して1を足す)

# パイプラインと組み合わせ
[ 1 2 3 4 5 ]
  == [ 2 ] * | MAP
  == [ 5 ] < NOT | FILTER
  == [ 0 ] + | FOLD
# → [ 24 ]  (6+8+10)

## 注意
- コードブロック（... |）またはワード名を指定
- 各要素に対して独立して実行されます"#,

        "FILTER" => r#"# FILTER - 条件に合う要素を抽出

## 機能
ベクタの各要素に対して条件コードを適用し、
trueを返した要素のみを残します。
該当する要素がない場合はNILを返します。

## 使用法
[ 要素... ] condition | FILTER       # コードブロックを直接指定
[ 要素... ] 'WORD' FILTER              # ワード名で指定
→ 条件を満たす要素のみのベクタ、または NIL

## 使用例
[ 1 2 3 4 5 6 ] [ 2 ] MOD [ 0 ] = NOT | FILTER
# → [ 1 3 5 ]  (奇数のみ抽出)

[ 10 5 20 3 15 ] [ 10 ] < NOT | FILTER
# → [ 10 20 15 ]  (10以上の要素)

# カスタムワードを使用
[ 5 ] < NOT | 'IS_LARGE' DEF
[ 1 3 8 2 7 ] 'IS_LARGE' FILTER
# → [ 8 7 ]  (5以上の要素)

# Nil Coalescingと組み合わせ
[ 1 2 3 ] [ 10 ] < NOT | FILTER => [ 0 ]
# → [ 0 ]  (該当なしでNIL、=> でデフォルト値)

## 注意
- コードブロック（... |）またはワード名を指定
- 条件コードはtrue/falseを返す必要があります
- 該当要素がない場合はNILを返します"#,

        "FOLD" => r#"# FOLD - 初期値付き畳み込み

## 機能
ベクタまたはスタック上の要素を、初期値から始めて二項演算コードで畳み込み、
単一の結果に集約します。REDUCEと異なり、初期値を明示的に指定できます。

## REDUCEとの違い
- REDUCE: 最初の要素を初期値として使用、空ベクタでエラー
- FOLD: 初期値を明示的に指定、空ベクタでは初期値をそのまま返す

## 畳み込みの順序
左から右へ順番に演算を適用します：
[ a b c ] [init] op | FOLD = ((init OP a) OP b) OP c

## 使用法（StackTopモード）
[ 要素... ] [ 初期値 ] code | FOLD   # コードブロックを直接指定
[ 要素... ] [ 初期値 ] 'ワード名' FOLD # ワード名で指定
→ 畳み込んだ結果を単一要素ベクタとして返す

## 使用法（Stackモード）
要素... [ 個数 ] [ 初期値 ] code | .. FOLD
→ スタック上のN個の要素を初期値から畳み込む

## 使用例
[ 1 2 3 4 ] [ 0 ] + | FOLD     # → [ 10 ]  (0+1+2+3+4)
[ 1 2 3 4 ] [ 1 ] * | FOLD     # → [ 24 ]  (1*1*2*3*4)
[ 1 2 3 ] [ 10 ] - | FOLD      # → [ 4 ]   (10-1-2-3)

# 空ベクタ（NIL）の場合は初期値がそのまま返る
NIL [ 42 ] + | FOLD            # → [ 42 ]

# カスタム演算
[ 1 2 3 ] [ 0 ] [ 2 ] * + | FOLD   # → [ 12 ]  (各要素を2倍して加算)

# Stackモード
[1] [2] [3] [3] [0] + | .. FOLD   # → [ 6 ]  (0+1+2+3)

## 畳み込みの動作（詳細）
[ 1 2 3 ] [ 0 ] + | FOLD の処理順序：
1. acc = 0 (初期値)
2. acc = acc + 1 = 1
3. acc = acc + 2 = 3
4. acc = acc + 3 = 6
5. 結果: [ 6 ]

## 注意
- 空ベクタ [ ] はNILとして扱われる
- 空ベクタ（NIL）に対しては初期値をそのまま返す（エラーにならない）
- 単一要素のベクタに対しても正常動作（init OP x を計算）
- 適用するコードは2つの値を受け取り、1つを返す必要がある
- 組み込みワード（+, -, *, /）とカスタムワードの両方が使用可能
- コードブロック（... |）またはワード名を指定"#,

        _ => return None,
    };
    Some(result.to_string())
}
