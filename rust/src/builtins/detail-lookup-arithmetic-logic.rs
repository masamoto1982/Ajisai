




pub(crate) fn lookup_detail_arithmetic_logic(name: &str) -> Option<String> {
    let result = match name {




        "+" => r#"# + - 加算

## 機能（StackTopモード）
2つのベクタの要素ごとの加算を行います。
片方が単一要素の場合、ブロードキャスト（全要素に適用）されます。

## 機能（Stackモード）
スタックから指定個数の要素を取得し、左から右へ順に加算します。

## 使用法（StackTopモード）
[ 要素... ] [ 要素... ] +
→ 要素ごとに加算した新しいベクタ

## 使用法（Stackモード）
要素... [ 個数 ] .. +
→ 指定個数の要素を加算した結果

## 使用例（StackTopモード）
[ 1 2 3 ] [ 4 5 6 ] +      # → [ 5 7 9 ]  (要素ごと)
[ 1 2 3 ] [ 10 ] +         # → [ 11 12 13 ]  (ブロードキャスト)
[ 5 ] [ 1 2 3 ] +          # → [ 6 7 8 ]  (逆向きブロードキャスト)

## 使用例（Stackモード）
[ 1 ] [ 2 ] [ 3 ] [ 3 ] .. +   # → [ 6 ]  (1+2+3)
[ 10 ] [ 5 ] [ 2 ] .. +        # → [ 12 ]  (10+5、デフォルト2個)

## 注意
- ベクタの長さが異なる場合（ブロードキャスト以外）はエラー
- 演算結果が入力と同じ場合（例：[0]との加算）はエラー"#,

        "-" => r#"# - - 減算

## 機能（StackTopモード）
2つのベクタの要素ごとの減算を行います。
片方が単一要素の場合、ブロードキャスト（全要素に適用）されます。

## 機能（Stackモード）
スタックから指定個数の要素を取得し、左から右へ順に減算します。

## 使用法（StackTopモード）
[ 要素... ] [ 要素... ] -
→ 要素ごとに減算した新しいベクタ

## 使用法（Stackモード）
要素... [ 個数 ] .. -
→ 指定個数の要素を減算した結果

## 使用例（StackTopモード）
[ 5 7 9 ] [ 1 2 3 ] -      # → [ 4 5 6 ]  (要素ごと)
[ 10 20 30 ] [ 5 ] -       # → [ 5 15 25 ]  (ブロードキャスト)
[ 100 ] [ 1 2 3 ] -        # → [ 99 98 97 ]  (逆向きブロードキャスト)

## 使用例（Stackモード）
[ 10 ] [ 3 ] [ 2 ] [ 3 ] .. -  # → [ 5 ]  (10-3-2)
[ 100 ] [ 25 ] [ 2 ] .. -      # → [ 75 ]  (100-25)

## 注意
- ベクタの長さが異なる場合（ブロードキャスト以外）はエラー
- 演算結果が入力と同じ場合（例：[0]との減算）はエラー"#,

        "*" => r#"# * - 乗算

## 機能（StackTopモード）
2つのベクタの要素ごとの乗算を行います。
片方が単一要素の場合、ブロードキャスト（全要素に適用）されます。

## 機能（Stackモード）
スタックから指定個数の要素を取得し、左から右へ順に乗算します。

## 使用法（StackTopモード）
[ 要素... ] [ 要素... ] *
→ 要素ごとに乗算した新しいベクタ

## 使用法（Stackモード）
要素... [ 個数 ] .. *
→ 指定個数の要素を乗算した結果

## 使用例（StackTopモード）
[ 1 2 3 ] [ 4 5 6 ] *      # → [ 4 10 18 ]  (要素ごと)
[ 1 2 3 ] [ 10 ] *         # → [ 10 20 30 ]  (ブロードキャスト)
[ 5 ] [ 2 3 4 ] *          # → [ 10 15 20 ]  (逆向きブロードキャスト)

## 使用例（Stackモード）
[ 2 ] [ 3 ] [ 4 ] [ 3 ] .. *   # → [ 24 ]  (2*3*4)
[ 5 ] [ 6 ] [ 2 ] .. *         # → [ 30 ]  (5*6)

## 注意
- ベクタの長さが異なる場合（ブロードキャスト以外）はエラー
- 演算結果が入力と同じ場合（例：[1]との乗算）はエラー"#,

        "/" => r#"# / - 除算

## 機能（StackTopモード）
2つのベクタの要素ごとの除算を行います。
片方が単一要素の場合、ブロードキャスト（全要素に適用）されます。
結果は分数として正確に保持されます。

## 機能（Stackモード）
スタックから指定個数の要素を取得し、左から右へ順に除算します。

## 使用法（StackTopモード）
[ 要素... ] [ 要素... ] /
→ 要素ごとに除算した新しいベクタ

## 使用法（Stackモード）
要素... [ 個数 ] .. /
→ 指定個数の要素を除算した結果

## 使用例（StackTopモード）
[ 10 20 30 ] [ 2 4 5 ] /   # → [ 5 5 6 ]  (要素ごと)
[ 10 20 30 ] [ 10 ] /      # → [ 1 2 3 ]  (ブロードキャスト)
[ 100 ] [ 2 4 5 ] /        # → [ 50 25 20 ]  (逆向きブロードキャスト)

## 使用例（Stackモード）
[ 100 ] [ 2 ] [ 5 ] [ 3 ] .. /  # → [ 10 ]  (100/2/5)
[ 20 ] [ 4 ] [ 2 ] .. /         # → [ 5 ]  (20/4)

## 注意
- ベクタの長さが異なる場合（ブロードキャスト以外）はエラー
- 0での除算はエラー
- 演算結果が入力と同じ場合（例：[1]での除算）はエラー"#,





        "=" => r#"# = - 等価比較

## 機能（StackTopモード）
2つの値を比較し、完全に等しいかどうかを判定します。
あらゆる型の値を比較可能（Number、String、Boolean、Vector、Nil）。

## 機能（Stackモード）
N個の要素を順に比較し、全て等しいかを判定します。

## 使用法
[ value1 ] [ value2 ] =
→ 等しければ [true]、異なれば [false]

## 使用例（StackTopモード）
[ 3 ] [ 3 ] =              # → [ true ]
[ 3 ] [ 5 ] =              # → [ false ]
[ 'hello' ] [ 'hello' ] =  # → [ true ]
[ 1 2 ] [ 1 2 ] =          # → [ true ]  (ベクタ全体を比較)
[ 1 2 ] [ 1 3 ] =          # → [ false ]

## 使用例（Stackモード）
[ 3 ] [ 3 ] [ 3 ] [ 3 ] .. =    # → [ true ]  (全て等しい)
[ 1 ] [ 2 ] [ 1 ] [ 3 ] .. =    # → [ false ] (1≠2)

## 注意
- ベクタ全体を1つの値として比較します
- 要素ごとの比較ではありません"#,

        "<" => r#"# < - 小なり比較

## 機能
2つのベクタの要素ごとに「左 < 右」を比較します。
要素数が等しい場合は要素ごとに比較、片方が単一要素の場合はブロードキャスト。

## 使用法
[ 要素... ] [ 要素... ] <
→ 各要素の比較結果（true/false）のベクタ

## 使用例
[ 1 2 3 ] [ 2 2 2 ] <      # → [ true false false ]
[ 1 2 3 ] [ 3 ] <          # → [ true true false ]  (ブロードキャスト)
[ 5 ] [ 1 2 3 4 5 ] <      # → [ false false false false false ]

## > の代替として
> 演算子は廃止されました。以下の方法で代替できます：
# 旧: [ 5 ] [ 3 ] >
# 新: [ 3 ] [ 5 ] <    (オペランドを逆に)
# または
# 新: [ 5 ] [ 3 ] < NOT

## 注意
- ベクタの長さが異なる場合（ブロードキャスト以外）はエラー"#,

        "<=" => r#"# <= - 小なりイコール比較

## 機能
2つのベクタの要素ごとに「左 <= 右」を比較します。
要素数が等しい場合は要素ごとに比較、片方が単一要素の場合はブロードキャスト。

## 使用法
[ 要素... ] [ 要素... ] <=
→ 各要素の比較結果（true/false）のベクタ

## 使用例
[ 1 2 3 ] [ 2 2 2 ] <=     # → [ true true false ]
[ 1 2 3 ] [ 3 ] <=         # → [ true true true ]  (ブロードキャスト)

## >= の代替として
>= 演算子は廃止されました。以下の方法で代替できます：
# 旧: [ 5 ] [ 3 ] >=
# 新: [ 3 ] [ 5 ] <=    (オペランドを逆に)
# または
# 新: [ 5 ] [ 3 ] <= NOT

## 注意
- ベクタの長さが異なる場合（ブロードキャスト以外）はエラー"#,

        ">" => r#"# > - 廃止された比較演算子

## 注意
この演算子は廃止されました。

## 代替方法
# 旧: [ 5 ] [ 3 ] >
# 新: [ 3 ] [ 5 ] <    (オペランドを逆に)
# または
# 新: [ 5 ] [ 3 ] < NOT

## 理由
「< と <= のみ使用する」方針により、数直線との対応が明確になり
コードの読みやすさが向上します。"#,

        ">=" => r#"# >= - 廃止された比較演算子

## 注意
この演算子は廃止されました。

## 代替方法
# 旧: [ 5 ] [ 3 ] >=
# 新: [ 3 ] [ 5 ] <=    (オペランドを逆に)
# または
# 新: [ 5 ] [ 3 ] <= NOT

## 理由
「< と <= のみ使用する」方針により、数直線との対応が明確になり
コードの読みやすさが向上します。"#,





        "AND" => r#"# AND - 論理積

## 機能
2つのベクタの要素ごとに論理積（AND）を計算します。
要素数が等しい場合は要素ごとに演算、片方が単一要素の場合はブロードキャスト。
`&` は AND の後置エイリアス（糖衣構文）です。

## 使用法
[ 要素... ] [ 要素... ] AND
→ 各要素の論理積のベクタ

[ 要素... ] [ 要素... ] &
→ AND と同じ意味（中置の `a & b` ではなく後置ワード）

## 使用例
[ true true false false ] [ true false true false ] AND
# → [ true false false false ]

[ true false true ] [ true ] AND
# → [ true false true ]  (ブロードキャスト)

[ true false true ] [ true ] &
# → [ true false true ]  (ANDの後置エイリアス)

## 注意
- ベクタの長さが異なる場合（ブロードキャスト以外）はエラー
- 要素はboolean型である必要があります"#,

        "OR" => r#"# OR - 論理和

## 機能
2つのベクタの要素ごとに論理和（OR）を計算します。
要素数が等しい場合は要素ごとに演算、片方が単一要素の場合はブロードキャスト。

## 使用法
[ 要素... ] [ 要素... ] OR
→ 各要素の論理和のベクタ

## 使用例
[ true true false false ] [ true false true false ] OR
# → [ true true true false ]

[ true false true ] [ false ] OR
# → [ true false true ]  (ブロードキャスト)

## 注意
- ベクタの長さが異なる場合（ブロードキャスト以外）はエラー
- 要素はboolean型である必要があります"#,

        "NOT" => r#"# NOT - 論理否定

## 機能
ベクタの各要素の論理否定（NOT）を計算します。

## 使用法
[ 要素... ] NOT
→ 各要素を否定したベクタ

## 使用例
[ true false true ] NOT    # → [ false true false ]
[ false ] NOT              # → [ true ]

## 注意
- 要素はboolean型である必要があります"#,

        _ => return None,
    };
    Some(result.to_string())
}
