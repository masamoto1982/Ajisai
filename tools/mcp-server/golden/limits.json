{
  "schemaVersion": 1,
  "comment": "Every entry of the server's declared `mcp.limits` must appear here exactly once, and selftest.js fails if the two key sets differ. A limit an agent plans against has to have pinned, observable behaviour at its boundary; one that is declared and never exercised is a claim, not a control.",
  "coverageKinds": {
    "boundary": "A pair of real sources: one just under the declared value that succeeds, one just over it that fails in the stated way. Run against the live server on every selftest.",
    "hostGate": "Enforced by the adapter before or around execution, so it is exercised through the server's own admission path rather than through a source program.",
    "injectedLimit": "Not reachable at the declared value within `wallTimeMs`, so the boundary is pinned in Rust with a small injected ceiling instead. The `rustTest` field names that test."
  },
  "limits": {
    "sourceBytes": {
      "coverage": "boundary",
      "declared": 65536,
      "under": { "kind": "generatedSource", "generator": "spaces", "bytes": 65536, "expect": { "/status": "ok" } },
      "over": {
        "kind": "generatedSource",
        "generator": "spaces",
        "bytes": 65537,
        "expect": { "/status": "hostError", "/error/code": "sourceTooLarge", "/error/limit": "sourceBytes", "/error/retryable": false }
      },
      "note": "Rejected by the adapter on UTF-8 byte length before a backend is entered. The engine applies the same 65536-byte ceiling as `ResourceLimitExceeded { resource: sourceBytes }` if a host ever forwards a larger program."
    },
    "wallTimeMs": {
      "coverage": "boundary",
      "declared": 5000,
      "under": { "kind": "source", "source": "2 SQRT 3 SQRT + 5 SQRT 7 SQRT + *", "expect": { "/status": "ok" } },
      "over": {
        "kind": "source",
        "source": "2 SQRT 3 SQRT + 5 SQRT 7 SQRT + * 11 SQRT 13 SQRT + * 17 SQRT 19 SQRT + *",
        "expect": { "/status": "hostError", "/error/code": "timeout", "/error/limit": "wallTimeMs", "/error/retryable": true }
      },
      "note": "The over case is a product of four two-radical sums. Its cost grows roughly twelvefold per additional factor, so it is many times over the ceiling rather than marginally over it; the under case completes in well under a second."
    },
    "responseBytes": {
      "coverage": "boundary",
      "declared": 1048576,
      "under": { "kind": "source", "source": "[ 0 1000 ] RANGE", "expect": { "/status": "ok" } },
      "over": {
        "kind": "source",
        "source": "[ 0 10000 ] RANGE",
        "expect": { "/status": "hostError", "/error/code": "responseTooLarge", "/error/limit": "responseBytes", "/error/retryable": false }
      },
      "note": "Both backends measure the same thing: UTF-8 bytes of the schema-1 envelope text. `backend/parity-test.js` runs this pair against both, which is what the native-only `maxBuffer` implementation could never have shown."
    },
    "executionSteps": {
      "coverage": "boundary",
      "declared": 100000,
      "under": {
        "kind": "source",
        "source": "{\n{ [ 0 ] > | [ 1 ] - DOWN }\n{ IDLE | [ 'done' ] } COND\n} 'DOWN' DEF\n1000 DOWN",
        "expect": { "/status": "ok" }
      },
      "over": {
        "kind": "source",
        "source": "{\n{ [ 0 ] > | [ 1 ] - DOWN }\n{ IDLE | [ 'done' ] } COND\n} 'DOWN' DEF\n200000 DOWN",
        "expect": {
          "/status": "error",
          "/diagnosis/why": "resourceLimit",
          "/aiDiagnostic/kind": "executionLimitExceeded",
          "/diagnosis/resourceLimit/resource": "executionSteps",
          "/diagnosis/resourceLimit/limit": 100000
        }
      }
    },
    "concurrentExecutions": {
      "coverage": "hostGate",
      "declared": 4,
      "note": "Exercised through `ExecutionGate` in selftest.js: the gate admits exactly its capacity, queues a further caller for up to CAPACITY_WAIT_MS, hands a released slot to the longest-waiting caller, and answers `capacityExhausted` with `retryable: true` only when the wait elapses."
    },
    "materializedElements": {
      "coverage": "boundary",
      "declared": 100000,
      "under": { "kind": "source", "source": "[ 0 99999 ] RANGE LENGTH", "expect": { "/status": "ok", "/stackDisplay/0": "100000/1" } },
      "over": {
        "kind": "source",
        "source": "[ 0 100001 ] RANGE",
        "expect": {
          "/status": "ok",
          "/stackDisplay/0": "NIL",
          "/stack/0/semantics/absence/reason": "spaceExhausted",
          "/errorFlowTrace/0/absence/reason": "spaceExhausted"
        }
      },
      "note": "A generative ceiling is a reason-carrying NIL, not an error: the program is well formed and the absence is recoverable with `^`."
    },
    "numericLiteralDigits": {
      "coverage": "boundary",
      "declared": 4096,
      "under": { "kind": "generatedSource", "generator": "digits", "digits": 4096, "expect": { "/status": "ok" } },
      "over": {
        "kind": "generatedSource",
        "generator": "digits",
        "digits": 4097,
        "expect": {
          "/status": "error",
          "/diagnosis/why": "resourceLimit",
          "/aiDiagnostic/kind": "resourceLimitExceeded",
          "/diagnosis/resourceLimit/resource": "numericLiteralDigits",
          "/diagnosis/resourceLimit/limit": 4096,
          "/diagnosis/resourceLimit/observed": 4097
        }
      }
    },
    "numericWork": {
      "coverage": "injectedLimit",
      "declared": 10000000,
      "rustTest": "rust/src/runtime_limits_tests.rs::runaway_numeric_work_is_charged_and_rejected_before_computing",
      "note": "Bounds exact algebraic (Tier 1) arithmetic only, and the per-operation estimate it charges is far cheaper than the operation. At 10,000,000 units the wall clock always arrives first — see the `wallTimeMs` over case, which spends about a thousand units before it is killed. The declared value is the configured ceiling and is reported truthfully; it is not a bound a caller can observe at this profile. Recalibrating the estimate is engine work tracked in docs/dev/mcp-readiness.md."
    },
    "bigintBits": {
      "coverage": "injectedLimit",
      "declared": 262144,
      "rustTest": "rust/src/runtime_limits_tests.rs::algebraic_term_explosion_is_rejected_at_a_low_injected_limit",
      "note": "Bounds the coefficient width of exact algebraic (Tier 1) values. Plain rational arithmetic does not pass through this guard, so a large integer product is bounded by `executionSteps` rather than by this ceiling. Reaching it through the algebraic path costs more wall time than the profile allows."
    },
    "algebraicTerms": {
      "coverage": "injectedLimit",
      "declared": 4096,
      "rustTest": "rust/src/runtime_limits_tests.rs::algebraic_term_explosion_is_rejected_at_a_low_injected_limit",
      "note": "Reaching 4096 terms needs twelve doublings of a two-radical sum; measured cost grows about twelvefold per doubling, so `wallTimeMs` fires many orders of magnitude earlier. Pinned with an injected three-term ceiling instead."
    }
  }
}
