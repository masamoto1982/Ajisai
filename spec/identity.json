{
  "schemaVersion": 1,
  "authority": "spec/language-semantics.md",
  "clauses": [
    "LANG.VALUES.DENOTATION",
    "LANG.VALUES.EXACT",
    "LANG.VALUES.TRUTH",
    "LANG.DICTIONARY.MUTATION",
    "LANG.FAILURE.TRICHOTOMY"
  ],
  "description": "When are two things the same? Ajisai answers that question at three levels and decides it by three different procedures, and the procedures do not all decide it completely. This file states the one law and what each level can actually deliver, so that a level which answers 'I cannot tell' is not mistaken for one that answered 'different'.",

  "law": {
    "statement": "Identity is denotation: two things are one thing exactly when they denote the same thing, whatever operations produced each of them. A procedure that decides identity therefore answers about denotation, and where it cannot decide, it answers that it cannot — never 'different'.",
    "outcomes": [
      {
        "id": "same",
        "meaning": "The two denote the same thing."
      },
      {
        "id": "different",
        "meaning": "The two denote different things."
      },
      {
        "id": "unknown",
        "meaning": "The procedure did not decide. This is not a third relation between the two things — it is a statement about the procedure. Nothing may read it as 'different'."
      }
    ],
    "why": "This is LANG.FAILURE.TRICHOTOMY's shape at a third place. A run answers with a value, a reasoned absence, or an error; a pre-execution check answers verified, cannot-verify, or violated; and a question of identity answers same, unknown, or different. In each, the middle outcome is the honest report of a procedure that did not reach an answer, and collapsing it into either neighbour is the mistake the trichotomy exists to prevent."
  },

  "levels": [
    {
      "id": "scalarAlgebraicField",
      "subject": "Two scalars in the multiquadratic field",
      "procedure": "Compare multiquadratic normal forms.",
      "decides": ["same", "different"],
      "total": true,
      "note": "LANG.VALUES.EXACT fixes this domain by a condition that includes deciding equality and order in finite time, so `unknown` is unreachable here — not because this clause forbids it but because a normal form that did not decide would not meet the condition.",
      "witnesses": [
        {
          "source": "8 SQRT 2 SQRT 2 SQRT ADD EQ",
          "expect": "TRUE",
          "note": "The specification's own example: sqrt(8) and sqrt(2)+sqrt(2) are one value, reached by different operations."
        },
        {
          "source": "2 SQRT 3 SQRT EQ",
          "expect": "FALSE",
          "note": "Decided, and decided negatively — `different` is a real answer at this level, not a default."
        }
      ]
    },
    {
      "id": "recordDenotation",
      "subject": "Two Records",
      "procedure": "Compare the key sequences position by position, then the value sequences, each pair by the procedure its own domain provides.",
      "decides": ["same", "different"],
      "total": true,
      "note": "A Record's identity is its two sequences (LANG.RECORDS.STRUCTURE): the same keys in the same order over the same values. Key order counts, so a Record built by WITH in one order is different from one built in another, and construction history beyond that is unobservable. Every key and value is compared by a procedure that decides, so the Record comparison decides too.",
      "witnesses": [
        {
          "source": "[ 'x' 'y' ] [ 1 2 ] RECORD [ 'x' 'y' ] [ 1 2 ] RECORD EQ",
          "expect": "TRUE",
          "note": "Two constructions, one value."
        },
        {
          "source": "[ 'x' 'y' ] [ 1 2 ] RECORD [ 'y' 'x' ] [ 2 1 ] RECORD EQ",
          "expect": "FALSE",
          "note": "The same correspondence in a different key order is a different Record: order is observable structure, as it is for a Vector."
        }
      ]
    },
    {
      "id": "userWordContentIdentity",
      "subject": "Two User Word definitions",
      "procedure": "Compare content identities — a digest over the normalized body with every reference replaced by the identity of its target.",
      "decides": ["same", "unknown"],
      "total": false,
      "unknownArrivesAs": {
        "kind": "hostJudgement",
        "note": "Comparing two definitions is not an operation the language has — no Word takes two Words and answers whether they are one. The question is asked by a host reading the identities it is given, so this level's `unknown` is not a value a program can hold and has no NIL reason. It is a rule about what a host may conclude: from equal identities, that the definitions are one; from unequal ones, nothing."
      },
      "soundness": "Equal identities imply equal denotations. This direction is what the host's import deduplication rests on (src/gui/interpreter-state-persistence.ts merges words that share an identity), so an unsound identity would silently merge two different Words in a reader's dictionary.",
      "incompleteness": "Unequal identities imply nothing. Two definitions that denote the same function can carry different identities, because the digest sees normalized structure and structure is finer than denotation. A consumer must read an identity mismatch as `unknown`, never as `different`.",
      "whyNotDecidable": "Deciding that two definitions denote the same function is a question about all of their inputs. Ajisai's totality makes each one a finite computation, but the space of inputs is not finite — a scalar is unbounded — so no digest, and no finite procedure of this kind, can answer `different`.",
      "invariantUnder": [
        {
          "id": "wordName",
          "note": "The name the definition is bound to.",
          "left": "[ 1 ADD ] 'IDA' DEF",
          "leftWord": "IDA",
          "right": "[ 1 ADD ] 'IDB' DEF",
          "rightWord": "IDB"
        },
        {
          "id": "aliasSpelling",
          "note": "A symbolic alias and its canonical Word are one name (LANG.SOURCE.NORMALIZE).",
          "left": "[ 1 ADD ] 'IDC' DEF",
          "leftWord": "IDC",
          "right": "[ 1 + ] 'IDD' DEF",
          "rightWord": "IDD"
        },
        {
          "id": "numericSpelling",
          "note": "A number is its value, not its lexeme (LANG.VALUES.DENOTATION applied inside the body).",
          "left": "[ 1 ADD ] 'IDE' DEF",
          "leftWord": "IDE",
          "right": "[ 1/1 ADD ] 'IDF' DEF",
          "rightWord": "IDF"
        },
        {
          "id": "layout",
          "note": "Whitespace is not part of a definition.",
          "left": "[ 1 ADD ] 'IDG' DEF",
          "leftWord": "IDG",
          "right": "[ 1   ADD ] 'IDH' DEF",
          "rightWord": "IDH"
        },
        {
          "id": "dependencyName",
          "note": "A reference is replaced by the identity of its target before hashing, so what a dependency is called does not reach the digest.",
          "left": "[ 1 ADD ] 'IDP' DEF\n[ IDP ] 'IDUSEP' DEF",
          "leftWord": "IDUSEP",
          "right": "[ 1 ADD ] 'IDQ' DEF\n[ IDQ ] 'IDUSEQ' DEF",
          "rightWord": "IDUSEQ"
        }
      ],
      "distinguishes": [
        {
          "id": "dependencyBody",
          "note": "A change to a dependency changes the identity of everything that depends on it, which is the property LANG.DICTIONARY.MUTATION names.",
          "left": "[ 1 ADD ] 'IDR' DEF\n[ IDR ] 'IDUSER' DEF",
          "leftWord": "IDUSER",
          "right": "[ 2 ADD ] 'IDS' DEF\n[ IDS ] 'IDUSES' DEF",
          "rightWord": "IDUSES"
        }
      ],
      "incompletenessWitness": {
        "note": "Adding zero changes no denotation and changes the structure, so these two denote one function and carry two identities. This is the witness that an identity mismatch means `unknown`.",
        "left": "[ 1 ADD ] 'IDX' DEF",
        "leftWord": "IDX",
        "right": "[ 1 ADD 0 ADD ] 'IDY' DEF",
        "rightWord": "IDY",
        "agreeOn": ["0", "1", "5", "1/2", "-1", "[ 1 2 3 ]"]
      }
    }
  ]
}
